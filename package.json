{
  "name": "kgtolbs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'static site, no build needed'",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
